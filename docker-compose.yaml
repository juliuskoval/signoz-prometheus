services:
  signoz-proxy:
    image: juliuskoval/signoz-proxy:latest
    container_name: signoz-proxy
    ports:
      - "8081:8081"
    networks:
      - signoz-net
    restart: unless-stopped
networks:
  signoz-net:
    external: true
